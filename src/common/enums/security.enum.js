export const HashApproachEnum = {
  bcrypt: "bcrypt",
  argon2:"argon2"
}

export const TokenTypeEnum = {
  access: 1,
  refresh: 2,
};

export const LogoutEnum = {
  all: 1,
  current: 2,
};